# Number of Observations in a Cross-Price Demand Model (Linear)

Number of Observations in a Cross-Price Demand Model (Linear)

## Usage

``` r
# S3 method for class 'cp_model_lm'
nobs(object, ...)
```

## Arguments

- object:

  A `cp_model_lm` object.

- ...:

  Additional arguments (unused).

## Value

Integer; observation count or `NA_integer_` if unavailable.
