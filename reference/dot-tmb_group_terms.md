# Group beezdemand_tmb fixed effects into testable blocks for anova()

Group beezdemand_tmb fixed effects into testable blocks for anova()

## Usage

``` r
.tmb_group_terms(object, terms = NULL, group_by = "auto")
```
