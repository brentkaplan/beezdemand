% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{residuals.beezdemand_tmb}
\alias{residuals.beezdemand_tmb}
\title{Residuals for a beezdemand_tmb fit}
\usage{
\method{residuals}{beezdemand_tmb}(
  object,
  type = c("response", "pearson"),
  scale = c("model", "natural"),
  level = c("subject", "population"),
  ...
)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} object.}

\item{type}{One of \code{"response"} (default) or \code{"pearson"}.}

\item{scale}{One of \code{"model"} (default) or \code{"natural"}.}

\item{level}{Reserved for TICKET-014. Currently \code{"subject"} only.}

\item{...}{Unused.}
}
\value{
Numeric vector of length \code{nobs(object)}.
}
\description{
Default returns response residuals (\code{y_on_scale - fitted}) on the model's
native scale. \code{type = "pearson"} divides by the residual SD on the model
scale (\code{exp(coef[["logsigma_e"]])}). Requesting \code{type = "pearson"} with
\code{scale = "natural"} falls back to \code{type = "response"} with a message
because a response-scale residual SD is not identified for the
exponential/zben variants without a separate variance assumption.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
head(residuals(fit))
head(residuals(fit, type = "pearson"))
}
}
\seealso{
\code{\link[=fitted.beezdemand_tmb]{fitted.beezdemand_tmb()}}, \code{\link[=augment.beezdemand_tmb]{augment.beezdemand_tmb()}}.
}
