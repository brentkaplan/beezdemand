% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{.tmb_subject_re_deviates}
\alias{.tmb_subject_re_deviates}
\title{Per-subject random-effect deviates for predict.beezdemand_tmb}
\usage{
.tmb_subject_re_deviates(object, newdata, re_parsed)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} fit.}

\item{newdata}{A data frame containing the model's \code{id} column.}

\item{re_parsed}{The fit's parsed random-effects specification
(\code{object$param_info$random_effects_parsed}); may be \code{NULL}.}
}
\value{
A list with \code{re_q0_contrib} and \code{re_alpha_contrib}, each a
numeric vector of length \code{nrow(newdata)}.
}
\description{
For each row of \code{newdata}, returns the random-effect contribution to the
Q0 and alpha linear predictors, looked up by \code{id}; errors if any id is
not a subject in the fit. Extracted from \code{\link[=.tmb_build_predicted_pars]{.tmb_build_predicted_pars()}} so
the population-level prediction path can skip it entirely.
}
\keyword{internal}
