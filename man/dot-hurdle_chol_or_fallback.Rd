% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle-demand.R
\name{.hurdle_chol_or_fallback}
\alias{.hurdle_chol_or_fallback}
\title{Cholesky Decomposition with Non-PD Fallback}
\usage{
.hurdle_chol_or_fallback(Sigma, sigma_diag)
}
\arguments{
\item{Sigma}{A candidate covariance matrix.}

\item{sigma_diag}{Numeric vector of per-RE variances for the diagonal
fallback (length must equal \code{nrow(Sigma)}).}
}
\value{
The upper-triangular Cholesky factor of \code{Sigma}, or of the
diagonal fallback when \code{Sigma} is not positive definite.
}
\description{
Internal helper shared by the RE-transform and marginal-prediction code
paths. Attempts \code{chol(Sigma)}; if \code{Sigma} is not positive definite (\code{chol()}
errors — near-boundary rhos, overflow, or \code{tanh(raw)} rounding to exactly
+/-1), falls back to the Cholesky factor of an uncorrelated diagonal
covariance built from \code{sigma_diag} and emits ONE classed warning, since the
returned/reported correlation estimates no longer describe the transformed
random effects or marginal draws that result (TICKET-061).
}
\keyword{internal}
