% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-within-subject.R
\name{.simulate_continuous_re_demand}
\alias{.simulate_continuous_re_demand}
\title{Simulate dose-response demand with a continuous within-subject random slope}
\usage{
.simulate_continuous_re_demand(
  n_subjects = 60,
  doses = c(-2, -1, 0, 1, 2),
  prices = c(0, 1, 2, 4, 8, 16, 24, 36),
  log_q0_pop = log(20),
  log_alpha_pop = log(0.006),
  b1_q0 = 0.1,
  b1_alpha = -0.15,
  sd_u_q0 = 0.3,
  sd_w_q0 = 0.1,
  sd_u_alpha = 0.3,
  sd_w_alpha = 0.1,
  rho_q0 = 0.3,
  rho_alpha = 0.3,
  sigma_e = 0.05,
  covariate_name = "dose_c",
  seed = NULL
)
}
\arguments{
\item{n_subjects}{Integer; number of subjects.}

\item{doses}{Numeric vector of within-subject covariate values seen by every
subject. Assumed centered (mean ~ 0); for dose ladders typically
\code{log10}-spaced then centered.}

\item{prices}{Numeric vector of prices each subject sees at every dose.}

\item{log_q0_pop, log_alpha_pop}{Numeric; population log-Q0 / log-alpha at the
reference covariate value (\code{dose_c = 0}).}

\item{b1_q0, b1_alpha}{Numeric; fixed dose slopes on log-Q0 / log-alpha.}

\item{sd_u_q0, sd_w_q0}{Numeric; SDs of the Q0 random intercept / slope.}

\item{sd_u_alpha, sd_w_alpha}{Numeric; SDs of the alpha random intercept / slope.}

\item{rho_q0, rho_alpha}{Numeric in (-1, 1); intercept-slope correlation on the
Q0 and alpha sides respectively.}

\item{sigma_e}{Numeric; SD of the lognormal observation noise on log-y.}

\item{covariate_name}{Character; name of the emitted covariate column
(default \code{"dose_c"}).}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble with columns \code{id} (factor), the covariate (\code{dose_c} by
default, numeric), \code{x} (price), and \code{y} (consumption). Long-format, one row
per (subject, dose, price). The true generating parameters are attached as
\code{attr(., "truth")}.
}
\description{
Sibling of \code{\link[=.simulate_within_subject_demand]{.simulate_within_subject_demand()}} for TICKET-051: instead of a
within-subject \emph{factor}, each subject is observed at several values of a
\emph{continuous} within-subject covariate (e.g. centered \code{log10} drug dose), and
both intensity and elasticity change with that covariate at a
subject-specific rate (a continuous random slope).
}
\details{
Data-generating process: for subject \code{i} at covariate value \code{d} (column
\code{dose_c}, assumed already centered) and price \code{p},
\deqn{\log Q_{0,i}(d) = \log Q_{0,\text{pop}} + \beta^{Q_0}_1 d + u^{Q_0}_i + w^{Q_0}_i d}
\deqn{\log \alpha_i(d) = \log \alpha_{\text{pop}} + \beta^{\alpha}_1 d + u^{\alpha}_i + w^{\alpha}_i d}
with per-subject random intercepts/slopes
\eqn{(u^{Q_0}_i, w^{Q_0}_i, u^{\alpha}_i, w^{\alpha}_i) \sim \mathcal{N}(0, \Sigma)},
and SND mean consumption
\eqn{\mu = Q_{0,i}(d)\exp(-\alpha_i(d)\,Q_{0,i}(d)\,p)} with multiplicative
lognormal observation noise \eqn{y = \mu \cdot \exp(\epsilon)},
\eqn{\epsilon \sim \mathcal{N}(0, \sigma_e^2)}.

\eqn{\Sigma} is block-diagonal across the Q0 and alpha parameter sides: the
Q0 intercept/slope pair correlates at \code{rho_q0}, the alpha pair at
\code{rho_alpha}, and the two sides are independent.

\strong{Noise caveat (TICKET-051 landmine #5).} The noise here is lognormal for
convenience (mirroring \code{test-tmb-recovery.R}), whereas the \code{simplified} /
\code{exponentiated} TMB likelihoods are Gaussian on raw Q. The fitted residual
SD therefore will \emph{not} match \code{sigma_e}; only \strong{mean-structure} recovery
(fixed slopes, RE intercept/slope SDs, and correlations) is a valid check.
}
\keyword{internal}
