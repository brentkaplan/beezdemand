% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{plot.beezdemand_tmb}
\alias{plot.beezdemand_tmb}
\title{Plot TMB Mixed-Effects Demand Model}
\usage{
\method{plot}{beezdemand_tmb}(
  x,
  type = c("demand", "individual", "parameters"),
  ids = NULL,
  prices = NULL,
  show_population = TRUE,
  show_observed = TRUE,
  show_pred = NULL,
  x_trans = c("log10", "log", "linear", "pseudo_log"),
  y_trans = NULL,
  inv_fun = NULL,
  x_limits = NULL,
  y_limits = NULL,
  x_lab = NULL,
  y_lab = NULL,
  style = c("modern", "apa"),
  observed_point_alpha = 0.3,
  observed_point_size = 1.5,
  pop_line_alpha = 1,
  pop_line_size = 1.2,
  ind_line_alpha = 0.3,
  ind_line_size = 0.5,
  ...
)
}
\arguments{
\item{x}{A \code{beezdemand_tmb} object.}

\item{type}{Character. One of \code{"demand"} (population curve with data),
\code{"individual"} (per-subject curves), \code{"parameters"} (parameter
distributions).}

\item{ids}{Character vector of subject IDs to plot (for individual type).}

\item{prices}{Optional numeric vector of prices for curve generation.}

\item{show_population}{Logical. Show population curve overlay.}

\item{show_observed}{Logical. Show observed data points.}

\item{show_pred}{Character. Which predictions to show: \code{"population"},
\code{"individual"}, or \code{"both"}. If \code{NULL} (default), determined by \code{type}.}

\item{x_trans}{Character. X-axis transformation.}

\item{y_trans}{Character. Y-axis transformation. If \code{NULL} (default),
uses \code{"pseudo_log"} which handles zero values gracefully.}

\item{inv_fun}{Optional function to back-transform y-axis. For \code{zben} and
\code{exponential} equations, the inverse link is applied automatically by
default so all demand plots are on the consumption scale.}

\item{x_limits, y_limits}{Numeric length-2 vectors for axis limits.}

\item{x_lab}{Character. X-axis label.}

\item{y_lab}{Character. Y-axis label.}

\item{style}{Character. Plot style: "modern" or "apa".}

\item{observed_point_alpha, observed_point_size}{Numeric. Aesthetics for
observed data points.}

\item{pop_line_alpha, pop_line_size}{Numeric. Aesthetics for population curve.}

\item{ind_line_alpha, ind_line_size}{Numeric. Aesthetics for individual curves.}

\item{...}{Additional arguments.}
}
\value{
A ggplot2 object.
}
\description{
Plot TMB Mixed-Effects Demand Model
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)

# Population demand curve
plot(fit, type = "demand")

# Individual curves for selected subjects
plot(fit, type = "individual", ids = c("19", "51"))

# Parameter distributions
plot(fit, type = "parameters")
}

}
