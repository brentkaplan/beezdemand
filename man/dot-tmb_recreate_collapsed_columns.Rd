% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{.tmb_recreate_collapsed_columns}
\alias{.tmb_recreate_collapsed_columns}
\title{Recreate \code{collapse_levels} factor columns in newdata}
\usage{
.tmb_recreate_collapsed_columns(object, newdata)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} fit.}

\item{newdata}{Data frame.}
}
\value{
\code{newdata} with any missing collapsed columns added.
}
\description{
\code{fit_demand_tmb(collapse_levels = ...)} fits on derived columns named
\verb{<factor>_Q0} / \verb{<factor>_alpha} that live only in \code{object$data}. A user
supplying newdata in the original shape has the original factor but not
those columns, and \code{predict()} rejected the rows as missing required
columns (F-BD6-4). The old-to-new level map is not stored on the fit, but
it is recoverable exactly from the training data, where both columns are
present. Columns already in \code{newdata} are left alone; an original level
unseen in training is left \code{NA} and caught by the later level check.
}
\keyword{internal}
