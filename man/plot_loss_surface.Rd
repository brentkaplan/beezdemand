% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_loss_surface}
\alias{plot_loss_surface}
\alias{plot_loss_surface.beezdemand_hurdle}
\alias{plot_loss_surface.beezdemand_tmb}
\alias{plot_loss_surface.beezdemand_nlme}
\title{Plot Loss Surface for Demand Model Parameters}
\usage{
plot_loss_surface(object, ...)

\method{plot_loss_surface}{beezdemand_hurdle}(
  object,
  resolution = 80,
  q0_range = NULL,
  alpha_range = NULL,
  fill_palette = "D",
  show_mle = TRUE,
  show_contours = FALSE,
  style = c("modern", "apa"),
  ...
)

\method{plot_loss_surface}{beezdemand_tmb}(
  object,
  resolution = 80,
  q0_range = NULL,
  alpha_range = NULL,
  fill_palette = "D",
  show_mle = TRUE,
  show_contours = FALSE,
  style = c("modern", "apa"),
  ...
)

\method{plot_loss_surface}{beezdemand_nlme}(
  object,
  resolution = 80,
  q0_range = NULL,
  alpha_range = NULL,
  fill_palette = "D",
  show_mle = TRUE,
  show_contours = FALSE,
  style = c("modern", "apa"),
  type = c("ssr", "marginal"),
  ...
)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Additional arguments passed to methods.}

\item{resolution}{Integer; grid resolution per axis (default 80).}

\item{q0_range}{Numeric vector of length 2; Q0 range (natural scale).
Default: MLE +/- 3 orders of magnitude.}

\item{alpha_range}{Numeric vector of length 2; alpha range (natural scale).
Default: MLE +/- 3 orders of magnitude.}

\item{fill_palette}{Character; viridis palette option (default \code{"D"}).}

\item{show_mle}{Logical; overlay MLE point (default \code{TRUE}).}

\item{show_contours}{Logical; add contour lines (default \code{FALSE}).}

\item{style}{Character; plot style, \code{"modern"} or \code{"apa"}.}

\item{type}{Character; loss surface to plot for NLME models. \code{"ssr"}
(default) uses sum-of-squared-residuals on price-aggregated means;
\code{"marginal"} uses a linearized marginal negative log-likelihood.}
}
\value{
A ggplot2 object.
}
\description{
Visualizes the sum-of-squared-residuals (SSR) surface over a grid of Q0 and
alpha values, holding other parameters (k, variance components) fixed at
their MLE. The SSR is computed on aggregated mean log-consumption by price
for the Part II (continuous) component of hurdle models.
}
\details{
\strong{Important:} This function computes SSR on price-aggregated means, not a
true profile likelihood. The resulting surface shows how well different
(Q0, alpha) pairs explain the average demand pattern, but does not account
for individual variation. For models with large random effects, the surface
may appear sharper than the full-data objective.

Supported model classes: \code{beezdemand_hurdle}, \code{beezdemand_tmb}, and
\code{beezdemand_nlme}. Models with factor covariates on Q0 or alpha are not
supported; use \code{\link[=get_demand_param_emms]{get_demand_param_emms()}} instead.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
plot_loss_surface(fit)
plot_loss_surface(fit, resolution = 50, show_contours = TRUE)
}

}
\seealso{
\code{\link[=plot_loss_profile]{plot_loss_profile()}} for 1D profile slices
}
