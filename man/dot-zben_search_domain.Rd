% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pmax-omax-engine.R
\name{.zben_search_domain}
\alias{.zben_search_domain}
\title{Analytic search domain for the zben expenditure maximum}
\usage{
.zben_search_domain(alpha_nat, q0_nat)
}
\arguments{
\item{alpha_nat, q0_nat}{Natural-scale zben parameters.}
}
\value{
Numeric \code{c(lower, upper)} search interval, or \code{NULL} when the
bound cannot be formed (non-finite or non-positive inputs).
}
\description{
The zben curve is \code{y_ll4(p) = q * exp(-t)} with \code{q = log10(Q0)} and the
dimensionless price \code{t = alpha * Q0 * p / q}; the natural-scale
expenditure is \verb{E = p * (10^(4 q exp(-t)) - 1)^(1/4)}. Writing
\verb{u = 4 ln(Q0) * exp(-t)}, the stationarity condition
\code{dE/dp = 0} reduces to \code{t * u / (4 (1 - exp(-u))) = 1}, and because
\code{u / (1 - exp(-u)) >= 1} for every \code{u > 0}, every stationary point
satisfies \code{t < 4}. All local (hence the global) expenditure maxima
therefore lie at prices below \code{4 * log10(Q0) / (alpha * Q0)}, whatever
prices were observed; the curve has at most two of them (Codex source
review, release-correctness audit 2026-09-06). Searching this domain
makes the reported \code{Pmax}/\code{Omax} independent of the observed price grid,
which a doubling expansion that stops at the first interior peak cannot
guarantee. Verified against a brute-force global search on 2 000 random
\verb{(Q0, alpha)} pairs (0 violations).
}
\keyword{internal}
