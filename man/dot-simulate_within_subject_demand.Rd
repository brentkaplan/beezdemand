% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-within-subject.R
\name{.simulate_within_subject_demand}
\alias{.simulate_within_subject_demand}
\title{Simulate within-subject demand data for TICKET-011 Phase 2 parity tests}
\usage{
.simulate_within_subject_demand(
  n_subjects = 30,
  n_conditions = 3,
  prices = c(0.1, 0.5, 1, 2, 5, 10, 20),
  log_q0_pop = log(20),
  log_alpha_pop = log(0.005),
  delta_q0 = NULL,
  delta_alpha = NULL,
  sigma_b = 0.3,
  sigma_d = 0.3,
  rho_bd = 0,
  sigma_e = 0.1,
  seed = NULL
)
}
\arguments{
\item{n_subjects}{Integer; number of subjects.}

\item{n_conditions}{Integer; number of within-subject condition levels
(named \code{"C1"}, \code{"C2"}, ...).}

\item{prices}{Numeric vector of prices each subject sees at every condition.}

\item{log_q0_pop}{Numeric; population log-Q0.}

\item{log_alpha_pop}{Numeric; population log-alpha.}

\item{delta_q0}{Numeric vector of length \code{n_conditions}; per-condition
shifts on log-Q0. Defaults to 0 for all conditions.}

\item{delta_alpha}{Numeric vector of length \code{n_conditions}; per-condition
shifts on log-alpha. Defaults to 0 for all conditions.}

\item{sigma_b}{Numeric; SD of per-(subject, condition) Q0 random deviation.}

\item{sigma_d}{Numeric; SD of per-(subject, condition) alpha random deviation.}

\item{rho_bd}{Numeric; correlation between b and d within (subject, condition).}

\item{sigma_e}{Numeric; residual SD on log-y.}

\item{seed}{Optional integer seed for reproducibility.}
}
\value{
A tibble with columns \code{id} (factor), \code{condition} (factor), \code{x}
(price), and \code{y} (consumption). Long-format, one row per
(subject, condition, price).
}
\description{
Generates long-format demand data where each subject is observed at every
price under every level of an in-subject \code{condition} factor. Used by the
Phase 2 parity tests to confirm that \code{fit_demand_tmb()} matches
\code{fit_demand_mixed()} on factor-expanded random-effects specifications
(\code{pdDiag(Q0+alpha~condition)}, \code{pdSymm(Q0+alpha~condition)} etc.).
}
\details{
Data-generating process: each subject \code{i} at condition \code{c} and price
\code{p} has consumption
\deqn{y_{i,c,p} = Q_{0,i,c} \cdot \exp(-\alpha_{i,c} \cdot Q_{0,i,c} \cdot p) \cdot \exp(\epsilon)}
where
\deqn{\log Q_{0,i,c} = \log Q_{0,\text{pop}} + \delta^{Q_0}_c + b_{i,c}}
\deqn{\log \alpha_{i,c} = \log \alpha_{\text{pop}} + \delta^{\alpha}_c + d_{i,c}}
with per-condition shifts \code{delta_q0[c]}, \code{delta_alpha[c]} and per-subject
per-condition random deviations \verb{(b_\{i,c\}, d_\{i,c\}) ~ N(0, Sigma)}.
}
\keyword{internal}
