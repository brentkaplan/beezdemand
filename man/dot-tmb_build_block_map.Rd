% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-demand.R
\name{.tmb_build_block_map}
\alias{.tmb_build_block_map}
\title{Build the block-structure metadata vectors for the Phase-2 TMB template}
\usage{
.tmb_build_block_map(re_parsed)
}
\arguments{
\item{re_parsed}{Output of \code{.normalize_re_input()}.}
}
\value{
A list of integer scalars and integer vectors describing the
block structure (see Conventions above).
}
\description{
Emits the integer vectors that tell the C++ template how to slice the
parameter vector into per-block covariance components and how to map
subject-level random effects through each block's Cholesky.
}
\details{
Conventions:
\itemize{
\item \code{block_types[b] = 0} for pdDiag, \code{1} for pdSymm.
\item \code{block_q0_offset[b]} / \code{block_alpha_offset[b]} are 0-indexed
starting columns within \code{Z_q0} / \code{Z_alpha} respectively.
\item \code{n_logsigma} is \code{sum(block_q0_dim + block_alpha_dim)} -- one
standard-deviation parameter per RE column across all blocks.
\item \code{n_rho} is \verb{sum(over pdSymm blocks: block_dim*(block_dim-1)/2)}
where \code{block_dim = block_q0_dim + block_alpha_dim}. pdDiag
blocks contribute 0 free correlations.
}
}
\keyword{internal}
