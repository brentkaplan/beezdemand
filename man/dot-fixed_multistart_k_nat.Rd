% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed-multistart.R
\name{.fixed_multistart_k_nat}
\alias{.fixed_multistart_k_nat}
\title{Resolve a natural-scale k value for the (Q0, Pmax) -> alpha mapping}
\usage{
.fixed_multistart_k_nat(k_production, k_arg, adf)
}
\arguments{
\item{k_production}{Numeric scalar; the production row's \code{K} value
(may be \code{NA} if the production fit was a total failure).}

\item{k_arg}{The \code{k} argument as supplied by the caller of
\code{fit_demand_fixed()}.}

\item{adf}{Single-subject data frame (post \code{CheckCols()}, columns
\code{id}/\code{x}/\code{y}) used as a last-resort fallback for \code{GetK()}.}
}
\value{
Numeric scalar natural-scale k (may be \code{NA_real_} if nothing
works).
}
\description{
Prefers the production fit's own recorded \code{K} (natural scale, already
back-transformed by \code{ExtractCoefs()}) since that is the exact value used
for this subject regardless of the caller's \code{k} mode (\code{"ind"}, \code{"fit"},
\code{"range"}, \code{"share"}, or a plain number). Falls back to the caller's \code{k}
if numeric, else to \code{GetK()} computed on the subject's own data -- both
are only used to seed a \emph{starting} alpha, so approximate is acceptable.
}
\keyword{internal}
