% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-demand.R
\name{.tmb_re_logsigma_starts}
\alias{.tmb_re_logsigma_starts}
\title{Per-RE-column starting log-SD values, scaled for continuous slopes}
\usage{
.tmb_re_logsigma_starts(re_parsed, data, base = log(0.5))
}
\arguments{
\item{re_parsed}{Output of \code{.normalize_re_input()}.}

\item{data}{Long-format fit data, or NULL (-> all columns at \code{base}).}

\item{base}{Numeric; flat starting log-SD (default \code{log(0.5)}).}
}
\value{
Numeric vector of length \code{n_logsigma}.
}
\description{
Returns the \code{logsigma} starting vector in canonical block order (per block:
Q0 columns then alpha columns). Intercept and factor-dummy columns keep the
flat \code{base} start (byte-identical to the historical \code{rep(log(0.5), n)}),
while a \emph{numeric} continuous-covariate slope column is started at
\code{base - log(spread)} so the slope's contribution \code{w_i * x_c} is on the same
scale as an intercept deviation (a slope SD lives on (covariate)^-1 units).
New behavior engages only when a numeric RE-RHS term is present, so factor
and intercept-only fits are unaffected (TICKET-051).
}
\keyword{internal}
