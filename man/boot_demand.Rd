% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boot-demand.R
\name{boot_demand}
\alias{boot_demand}
\title{Bootstrap Confidence Intervals for Derived Demand Metrics}
\usage{
boot_demand(
  fit,
  statistics = c("Pmax", "Omax", "EV"),
  method = c("parametric"),
  R = 1000L,
  ci_level = 0.95,
  at = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{beezdemand_tmb} object. NLME (\code{beezdemand_nlme}) and
hurdle fits are not supported in this version and raise an informative error.}

\item{statistics}{Character vector; any of
\code{c("Pmax", "Omax", "Qmax", "EV", "elasticity_at_pmax")}. Default
\code{c("Pmax", "Omax", "EV")}.}

\item{method}{Resampling scheme. Only \code{"parametric"} is available in this
version (nonparametric subject resampling is planned).}

\item{R}{Integer number of bootstrap draws; minimum 100, default 1000.}

\item{ci_level}{Confidence level for the empirical-quantile interval
(default 0.95).}

\item{at}{Optional named list of factor-level filters / continuous-covariate
value overrides, with the same shape as the \code{at} argument of
\code{\link{calc_group_metrics}}. When \code{NULL} (default) all factor cells are
enumerated; supplying \code{at} conditions to (or filters) the requested cell(s).}

\item{seed}{Optional integer seed for reproducible draws. The caller's RNG
state is left unperturbed.}

\item{...}{Reserved for future extension; must be empty. Unknown arguments
(e.g. a misspelled \code{statistics}) raise an error rather than being silently
ignored.}
}
\value{
A tibble with one row per \verb{(statistic, condition)}:
\describe{
\item{statistic}{Metric name.}
\item{condition}{Factor-cell label (e.g. \code{"gender=Male"}); \code{NA} when
the fit has no factors.}
\item{estimate}{Point estimate (from the coefficient vector and point \code{k}).}
\item{conf.low, conf.high}{Empirical-quantile interval bounds.}
\item{level}{The confidence level used.}
}
The bootstrap settings are attached as attributes \code{"method"}, \code{"R"}, and
\code{"seed"}, plus \code{"n_nonfinite"} (a per-row count of draws excluded as
non-finite).
}
\description{
Computes confidence intervals on derived demand metrics (Pmax, Omax, Qmax,
EV, elasticity-at-Pmax) for a TMB mixed-effects demand fit, via a parametric
bootstrap. Draws of the fixed-effect parameter vector are taken from the
joint asymptotic Gaussian posterior \eqn{N(\hat\beta, \hat\Sigma)}, mapped to
per-condition \eqn{(Q_0, \alpha, k)} through the model's fixed-effect design,
passed through the canonical Pmax/Omax engine, and summarized by empirical
quantiles.

For a factor-expanded fit, one CI is returned per factor cell (e.g. one row
per \code{gender} level); for an intercept-only fit, one row per statistic with
\code{condition = NA}. The per-cell point estimate reproduces
\code{\link{calc_group_metrics}(fit, at = cell)}; the bootstrap supplies only
the interval.
}
\details{
The parametric bootstrap is asymptotically equivalent to the delta method but
avoids its linearization, so it is preferable for the
strongly nonlinear derived metrics (Pmax/Omax via Lambert-W). Draws are
fixed-effect-only (population / per-condition metrics); per-subject metric CIs
would require random-effect-aware draws and are out of scope for now.

When \code{k} is estimated, its uncertainty is propagated (the \code{log_k} column is in
the draw matrix); when \code{k} is fixed, the fixed value is used. The point
estimate always uses the point \code{k}, matching \code{calc_group_metrics()}.

Note that percentile intervals of a nonlinear transform are not guaranteed to
bracket the point estimate; \code{conf.low <= conf.high} always holds, but
\verb{conf.low <= estimate <= conf.high} may not at boundary cases.

Some draws can leave a metric's domain (e.g. Pmax is undefined when a drawn
\code{k} falls below the Lambert-W threshold). Such non-finite draws are excluded
from the quantiles; the per-row count of excluded draws is recorded in
\code{attr(x, "n_nonfinite")} (so the realized draw count is \code{R} minus that). If
\emph{every} draw of a requested metric/condition is non-finite, an error is
raised because the interval is undefined.
}
\examples{
\donttest{
data(apt, package = "beezdemand")
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
boot_demand(fit, statistics = c("Pmax", "Omax", "EV"), R = 500, seed = 1)
}

}
\seealso{
\code{\link{calc_group_metrics}}, \code{\link{fit_demand_tmb}},
\code{\link{confint.beezdemand_tmb}}
}
