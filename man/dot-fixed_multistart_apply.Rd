% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed-multistart.R
\name{.fixed_multistart_apply}
\alias{.fixed_multistart_apply}
\title{Apply the multi-start rescue protocol to a completed FitCurves() run}
\usage{
.fixed_multistart_apply(
  results,
  fits,
  predictions,
  data_used,
  equation,
  k,
  agg = NULL,
  param_space,
  multistart,
  S,
  dots = list()
)
}
\arguments{
\item{results}{\code{dfres} data frame from the production \code{FitCurves()} call.}

\item{fits}{\code{fits} list from the production call.}

\item{predictions}{\code{newdats} list from the production call.}

\item{data_used}{\code{adfs} list from the production call.}

\item{equation}{Canonical (post \code{normalize_equation()}) equation name.}

\item{k}{The \code{k} argument as supplied to \code{fit_demand_fixed()}.}

\item{agg}{The \code{agg} argument as supplied to \code{fit_demand_fixed()}
(\code{NULL}, \code{"Mean"}, or \code{"Pooled"}). Codex 2F review fold, item 3: this
MUST be passed through unchanged to each rescue \code{FitCurves()} call
(never hard-coded to \code{NULL}) -- for \code{agg = "Pooled"}, \code{data_used[[i]]}
is the raw (duplicated-price) stacked data, and \code{FitCurves()}'s own
\code{agg = "Pooled"} handling is what builds the deduplicated table
\code{GetEmpirical()} needs; without it, \code{GetEmpirical()} hard-errors on
"Duplicates found where id = pooled".}

\item{param_space}{\code{"natural"} or \code{"log10"}.}

\item{multistart}{Logical; multi-start on/off.}

\item{S}{Integer budget, or \code{NULL} to use the tiered default.}

\item{dots}{Named list of additional arguments forwarded from
\code{fit_demand_fixed()}'s \code{...} (e.g. \code{lobound}, \code{hibound},
\code{constrainq0}) -- passed through to each rescue \code{FitCurves()} call,
with any user-supplied \code{startq0}/\code{startalpha} stripped (the sampled
starts replace them for rescue attempts).}
}
\value{
List with elements \code{results}, \code{fits}, \code{predictions},
\code{data_used} (post-rescue, same shapes as the inputs) and
\code{multistart_info} (settings + per-subject summary data frame, for the
\code{beezdemand_fixed} object's \verb{$multistart} field).
}
\description{
Called from \code{fit_demand_fixed()} after the production \code{FitCurves()} call
but before its success/failure bookkeeping, so downstream logic (which
derives \code{results$converged} from \code{results$converged_strict}) sees the
post-rescue verdicts automatically.
}
\keyword{internal}
