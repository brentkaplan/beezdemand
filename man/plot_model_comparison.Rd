% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_model_comparison}
\alias{plot_model_comparison}
\title{Compare Parameter Estimates Across Models}
\usage{
plot_model_comparison(
  ...,
  model_list = NULL,
  labels = NULL,
  parameters = c("Q0", "alpha"),
  conf_level = 0.95,
  style = c("modern", "apa")
)
}
\arguments{
\item{...}{Fitted model objects.}

\item{model_list}{Optional named list of models.}

\item{labels}{Character vector of model labels.}

\item{parameters}{Character vector of parameter names to compare
(default \code{c("Q0", "alpha")}).}

\item{conf_level}{Numeric; confidence level for intervals (default 0.95).}

\item{style}{Character; \code{"modern"} or \code{"apa"}.}
}
\value{
A ggplot2 object.
}
\description{
Creates a forest plot (coefficient plot) comparing parameter estimates and
confidence intervals across multiple fitted demand models.
}
\details{
Uses \code{tidy()} methods to extract parameter estimates and standard errors.
Confidence intervals are computed as estimate +/- z * SE. Parameters are
matched by the \code{term} column from \code{tidy()} output.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
plot_model_comparison(fit, labels = "Hurdle")
}

}
