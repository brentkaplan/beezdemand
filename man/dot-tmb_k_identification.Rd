% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{.tmb_k_identification}
\alias{.tmb_k_identification}
\title{Screen a free-k TMB fit for the signature of an unidentified k}
\usage{
.tmb_k_identification(object)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} fit.}
}
\value{
\code{NULL} when k was not estimated as a free parameter. Otherwise a list
with the fitted \code{k} and \code{log_k}, the largest decay exponent
\code{alpha * Q0 * price} over the fit (\code{decay_exponent_max}), the smallest
fitted \code{alpha_min}, a character vector of \code{reasons}, \code{at_boundary}
(\code{"log_k"} when it rests on a user-supplied optimizer bound, otherwise
\code{character(0)}), and a \code{severity} of \code{"warn"}, \code{"suspect"} or \code{"none"}.
}
\description{
For the k-bearing equations the response depends on k through
\code{k * (exp(-alpha * Q0 * price) - 1)}. While \code{alpha * Q0 * price} stays small
that term is linear in price with slope \code{k * alpha}, so only the product is
identified: k is pinned down by the curvature that appears as consumption
approaches its floor. On data that never get there a free k can drift
arbitrarily far with a compensating alpha.

This is a heuristic screen for that signature rather than a formal
identification test. A \code{"none"} severity means only that nothing was
detected.
}
\keyword{internal}
