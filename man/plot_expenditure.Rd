% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_expenditure}
\alias{plot_expenditure}
\alias{plot_expenditure.beezdemand_hurdle}
\alias{plot_expenditure.beezdemand_tmb}
\title{Plot Expenditure Curves}
\usage{
plot_expenditure(object, ...)

\method{plot_expenditure}{beezdemand_hurdle}(
  object,
  prices = NULL,
  n_points = 200,
  show_pmax = TRUE,
  show_omax = TRUE,
  x_trans = c("log10", "log", "linear", "pseudo_log"),
  free_trans = 0.01,
  x_lab = "Price",
  y_lab = "Expenditure (P × Q)",
  style = c("modern", "apa"),
  ...
)

\method{plot_expenditure}{beezdemand_tmb}(
  object,
  prices = NULL,
  n_points = 200,
  show_pmax = TRUE,
  show_omax = TRUE,
  x_trans = c("log10", "log", "linear", "pseudo_log"),
  free_trans = 0.01,
  x_lab = "Price",
  y_lab = "Expenditure (P × Q)",
  style = c("modern", "apa"),
  ...
)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Additional arguments passed to methods.}

\item{prices}{Numeric vector of prices. If \code{NULL}, uses a smooth grid
spanning the observed price range.}

\item{n_points}{Integer; number of grid points (default 200).}

\item{show_pmax}{Logical; show Pmax vertical line (default \code{TRUE}).}

\item{show_omax}{Logical; show Omax horizontal line (default \code{TRUE}).}

\item{x_trans}{Character; x-axis transformation (default \code{"log10"}).}

\item{free_trans}{Numeric; replacement for price = 0 on log scales.}

\item{x_lab, y_lab}{Axis labels.}

\item{style}{Character; \code{"modern"} or \code{"apa"}.}
}
\value{
A ggplot2 object.
}
\description{
Plots the expenditure curve (Price x Consumption) derived from the fitted
demand model. Optionally overlays Pmax and Omax reference lines.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
plot_expenditure(fit)
}

}
