% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_demand_overlay}
\alias{plot_demand_overlay}
\title{Overlay Demand Curves from Multiple Models}
\usage{
plot_demand_overlay(
  ...,
  model_list = NULL,
  labels = NULL,
  prices = NULL,
  n_points = 200,
  x_trans = c("log10", "log", "linear", "pseudo_log"),
  free_trans = 0.01,
  inv_fun = identity,
  x_lab = "Price",
  y_lab = "Consumption",
  style = c("modern", "apa")
)
}
\arguments{
\item{...}{Fitted model objects (named or unnamed).}

\item{model_list}{Optional named list of models (combined with \code{...}).}

\item{labels}{Character vector of model labels for the legend.}

\item{prices}{Numeric vector of prices. Default uses the union of observed
prices across all models.}

\item{n_points}{Integer; number of points for smooth curves (default 200).}

\item{x_trans}{Character; x-axis transformation (default \code{"log10"}).}

\item{free_trans}{Numeric; replacement for price = 0 on log scales.}

\item{inv_fun}{Function to back-transform consumption values (e.g.,
\link{ll4_inv} for LL4-transformed models). Default is \link{identity}.}

\item{x_lab, y_lab}{Axis labels.}

\item{style}{Character; \code{"modern"} or \code{"apa"}.}
}
\value{
A ggplot2 object.
}
\description{
Plots population-level demand curves from multiple fitted models on the same
axes for visual comparison.
}
\examples{
\donttest{
data(apt)
fit1 <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
# Compare with a 3-RE model:
# fit2 <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id",
#   random_effects = c("zeros", "q0", "alpha"))
# plot_demand_overlay(fit1, fit2, labels = c("2-RE", "3-RE"))
plot_demand_overlay(fit1, labels = c("Hurdle"))
}

}
