% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{model.matrix.beezdemand_tmb}
\alias{model.matrix.beezdemand_tmb}
\title{Design matrices for a beezdemand_tmb fit}
\usage{
\method{model.matrix}{beezdemand_tmb}(object, what = NULL, ...)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} object.}

\item{what}{\code{NULL} (default) returns the full named list. Otherwise one
of \code{"X_q0"}, \code{"X_alpha"}, \code{"Z_q0"}, \code{"Z_alpha"}.}

\item{...}{Unused.}
}
\value{
Named list of numeric matrices, or a single numeric matrix when
\code{what} is set. \code{NULL} (with a message) when a degenerate Z is requested.
}
\description{
By default returns a named list of all four design matrices the TMB
template consumed: \code{X_q0}, \code{X_alpha}, \code{Z_q0}, \code{Z_alpha}. Use \code{what}
to select a single matrix. \code{X_q0} and \code{X_alpha} are zero-copy
references to \code{fit$formula_details}; \code{Z_q0} and \code{Z_alpha} are
recomputed via the internal builder.
}
\details{
Returning a named list (vs the single matrix \code{lm}/\code{lme4} return) is
intentional: the TMB tier has two fixed-effect linear predictors
(one per nonlinear parameter), not one.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
str(model.matrix(fit))
head(model.matrix(fit, what = "X_q0"))
}
}
\seealso{
\code{\link[=formula.beezdemand_tmb]{formula.beezdemand_tmb()}}.
}
