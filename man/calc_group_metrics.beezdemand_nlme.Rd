% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-methods.R
\name{calc_group_metrics.beezdemand_nlme}
\alias{calc_group_metrics.beezdemand_nlme}
\title{Population-level demand metrics for a mixed-effects NLME fit}
\usage{
\method{calc_group_metrics}{beezdemand_nlme}(object, at = NULL, ...)
}
\arguments{
\item{object}{A \code{beezdemand_nlme} object from \code{\link[=fit_demand_mixed]{fit_demand_mixed()}}.}

\item{at}{Optional named list conditioning continuous covariates / factor
levels (same shape as the \code{beezdemand_tmb} method). Covariates default to
their training mean; factors are marginalized with equal weights unless a
level is supplied.}

\item{...}{Unused.}
}
\value{
A flat list with scalar \code{Pmax}, \code{Omax}, \code{Qmax},
\code{elasticity_at_pmax}, character \code{method}, and \code{conditioned_on} (a list of
\verb{$covariates} and/or \verb{$factors}, or \code{NULL} when the fit has neither).
}
\description{
Computes parameter-first-marginalized Pmax, Omax, Qmax, and
elasticity-at-Pmax for a \code{\link[=fit_demand_mixed]{fit_demand_mixed()}} model, mirroring the return
contract of \code{\link[=calc_group_metrics]{calc_group_metrics()}} for \code{beezdemand_tmb} fits: a flat scalar
list, NOT a tibble.
}
\details{
Fixed-effect log-Q0 and log-alpha estimated marginal means are averaged
across the reference grid (continuous covariates at their training mean by
default, factor levels equally weighted) on the natural scale (a geometric
mean), then the scalar metrics are derived from the marginalized parameters
via \code{\link[=beezdemand_calc_pmax_omax]{beezdemand_calc_pmax_omax()}}. \code{model_type} follows the equation form:
\code{"exponentiated"} (which carries a range parameter \code{k}) uses the Hursh &
Silberberg solution; \code{"zben"}/\code{"simplified"} use the simplified (SND)
solution.
}
\examples{
\donttest{
data(apt_full, package = "beezdemand")
apt_full$y_ll4 <- ll4(apt_full$y, lambda = 4)
fit <- fit_demand_mixed(
  apt_full, equation_form = "zben", factors = "gender",
  y_var = "y_ll4", x_var = "x", id_var = "id")
calc_group_metrics(fit)
calc_group_metrics(fit, at = list(gender = "Male"))
}

}
\seealso{
\code{\link[=calc_group_metrics]{calc_group_metrics()}}, \code{\link[=get_demand_param_emms]{get_demand_param_emms()}}
}
\keyword{internal}
