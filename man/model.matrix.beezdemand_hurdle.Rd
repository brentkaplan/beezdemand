% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hurdle-methods.R
\name{model.matrix.beezdemand_hurdle}
\alias{model.matrix.beezdemand_hurdle}
\title{Design matrices for a beezdemand_hurdle fit}
\usage{
\method{model.matrix}{beezdemand_hurdle}(object, what = NULL, ...)
}
\arguments{
\item{object}{A \code{beezdemand_hurdle} object.}

\item{what}{\code{NULL} (default) returns the full named list. Otherwise one of
\code{"X_binary"} or \code{"X_consumption"}.}

\item{...}{Unused.}
}
\value{
Named list of numeric matrices, or a single matrix when \code{what}
is set.
}
\description{
Hurdle currently has intercept-only Part I and Part II linear
predictors, so \code{X_binary} and \code{X_consumption} are each a single column
of ones with \code{nobs(fit)} rows. Returned for parity with
\code{\link[=model.matrix.beezdemand_tmb]{model.matrix.beezdemand_tmb()}}. Future support for factor / covariate
effects on hurdle components will enrich these matrices without
changing the API.
}
