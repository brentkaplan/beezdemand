% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-methods.R
\name{glance.beezdemand_nlme}
\alias{glance.beezdemand_nlme}
\title{Glance method for beezdemand_nlme}
\usage{
\method{glance}{beezdemand_nlme}(x, ...)
}
\arguments{
\item{x}{A beezdemand_nlme object}

\item{...}{Additional arguments (ignored)}
}
\value{
A one-row tibble of model statistics with columns:
\itemize{
\item \code{model_class}: "beezdemand_nlme"
\item \code{backend}: "nlme"
\item \code{equation_form}: The equation form used
\item \code{nobs}: Number of observations
\item \code{n_subjects}: Number of subjects
\item \code{n_random_effects}: Number of random-effect terms (e.g. 2 for
\code{Q0 + alpha ~ 1})
\item \code{converged}: Operational convergence status. \code{TRUE} when the final fit is
usable for inference — i.e. \code{apVar} (nlme's approximate covariance of the
variance-covariance parameters) is positive-definite AND there is no
terminal error. Alias for \code{final_fit_ok}. As of TICKET-020 this is no
longer flipped to \code{FALSE} by iteration-level optimizer warnings (see
\code{fit_warned}).
\item \code{final_fit_ok}: The canonical usable-for-inference gate (\code{apVar} PD and no
terminal error); identical to \code{converged}. NLME-only.
\item \code{fit_warned}: Diagnostic flag — \code{TRUE} when nlme emitted iteration-level
convergence warnings (false convergence, singular, step-halving, iteration
limit, ...) during PNLS-LME alternation. Informational only; does not gate
\code{converged}. NLME-only.
\item \code{logLik}, \code{AIC}, \code{BIC}: Model fit statistics
\item \code{sigma}: Residual standard error (NLME-only)
}

The shared canonical columns (through \code{converged}, \code{logLik}, \code{AIC}, \code{BIC})
match \code{\link[=glance.beezdemand_tmb]{glance.beezdemand_tmb()}}, so backend-agnostic code needs no dispatch
glue; \code{final_fit_ok} and \code{fit_warned} are additive NLME-only diagnostics.
}
\description{
Glance method for beezdemand_nlme
}
