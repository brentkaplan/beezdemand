% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_re_diagnostics}
\alias{plot_re_diagnostics}
\alias{plot_re_diagnostics.beezdemand_hurdle}
\alias{plot_re_diagnostics.beezdemand_tmb}
\title{Diagnostic Plots for Random Effects}
\usage{
plot_re_diagnostics(object, ...)

\method{plot_re_diagnostics}{beezdemand_hurdle}(
  object,
  which = c("all", "zeros", "q0", "alpha"),
  style = c("modern", "apa"),
  ...
)

\method{plot_re_diagnostics}{beezdemand_tmb}(
  object,
  which = c("all", "q0", "alpha"),
  style = c("modern", "apa"),
  ...
)
}
\arguments{
\item{object}{A fitted model object with random effects.}

\item{...}{Additional arguments (ignored).}

\item{which}{Character; which random effects to diagnose. One of \code{"all"}
(default), \code{"zeros"}, \code{"q0"}, or \code{"alpha"}.}

\item{style}{Character; \code{"modern"} or \code{"apa"}.}
}
\value{
A ggplot2/patchwork object, or a list of ggplot2 objects if
patchwork is not installed.
}
\description{
Creates diagnostic panels for random effects: histogram and Q-Q plot for
each selected random effect, plus (for the zeros RE) a comparison of
observed vs predicted proportion of zeros across prices.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
plot_re_diagnostics(fit)
plot_re_diagnostics(fit, which = "zeros")
}

}
