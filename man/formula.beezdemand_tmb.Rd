% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{formula.beezdemand_tmb}
\alias{formula.beezdemand_tmb}
\title{Formula for a beezdemand_tmb fit}
\usage{
\method{formula}{beezdemand_tmb}(x, ...)
}
\arguments{
\item{x}{A \code{beezdemand_tmb} object.}

\item{...}{Unused.}
}
\value{
Named list \code{list(Q0, alpha, random)}. \code{Q0} and \code{alpha} are
one-sided formulas built from \code{fit$formula_details}. \code{random} is
the original \code{random_effects} argument value (round-trippable back
to \code{fit_demand_tmb()}).
}
\description{
Returns the fixed-effect RHS formulas for Q0 and alpha plus the
original random-effect specification preserved at fit time. The
Q0 and alpha formulas may differ when \code{collapse_levels} was used
to collapse factor levels asymmetrically.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
formula(fit)
}
}
\seealso{
\code{\link[=model.matrix.beezdemand_tmb]{model.matrix.beezdemand_tmb()}}, \code{\link[=update.beezdemand_tmb]{update.beezdemand_tmb()}}.
}
