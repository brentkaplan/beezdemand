% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed-multistart.R
\name{.fixed_multistart_sample_qp}
\alias{.fixed_multistart_sample_qp}
\title{Draw S - 1 log-uniform (Q0, Pmax) sampled starts for one subject}
\usage{
.fixed_multistart_sample_qp(x, y, n)
}
\arguments{
\item{x}{Numeric price vector for the subject.}

\item{y}{Numeric consumption vector for the subject.}

\item{n}{Number of starts to draw (\code{S - 1}).}
}
\value{
List with elements \code{q0} and \code{pmax}, each length \code{n} (or length 0
if sampling is not possible, e.g. no strictly-positive observations).
}
\description{
Sampling region mirrors the qualtrics-panel-discounting-2 Tier-1
reference implementation
(\code{R/battery/11-multistart-protocol.R::sample_starts()}):
\verb{Q0 ~ log-uniform[0.25 * max(y+), 4 * max(y+)]},
\verb{Pmax ~ log-uniform[min(x+) / 2, 4 * max(x+)]}, where \verb{y+}/\verb{x+} are the
subject's strictly-positive observed consumption/price values.
}
\keyword{internal}
