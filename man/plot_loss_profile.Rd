% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-viz.R
\name{plot_loss_profile}
\alias{plot_loss_profile}
\alias{plot_loss_profile.beezdemand_hurdle}
\alias{plot_loss_profile.beezdemand_tmb}
\title{Plot Loss Profile for a Single Parameter}
\usage{
plot_loss_profile(object, ...)

\method{plot_loss_profile}{beezdemand_hurdle}(
  object,
  parameter = c("both", "q0", "alpha"),
  resolution = 200,
  range = c(-3, 3),
  style = c("modern", "apa"),
  ...
)

\method{plot_loss_profile}{beezdemand_tmb}(
  object,
  parameter = c("both", "q0", "alpha"),
  resolution = 200,
  range = c(-3, 3),
  style = c("modern", "apa"),
  ...
)
}
\arguments{
\item{object}{A fitted model object.}

\item{...}{Additional arguments passed to methods.}

\item{parameter}{Character; which parameter to profile: \code{"q0"}, \code{"alpha"},
or \code{"both"} (default).}

\item{resolution}{Integer; number of grid points (default 200).}

\item{range}{Numeric vector of length 2; range in log10 units relative to
MLE (default \code{c(-3, 3)}).}

\item{style}{Character; plot style, \code{"modern"} or \code{"apa"}.}
}
\value{
A ggplot2 object. If \code{parameter = "both"} and patchwork is
available, returns a combined patchwork object.
}
\description{
Plots 1D slices of the SSR surface, fixing one parameter at the MLE and
varying the other.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_hurdle(apt, y_var = "y", x_var = "x", id_var = "id")
plot_loss_profile(fit, parameter = "q0")
plot_loss_profile(fit, parameter = "both")
}

}
