% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{fitted.beezdemand_tmb}
\alias{fitted.beezdemand_tmb}
\title{Fitted values for a beezdemand_tmb fit}
\usage{
\method{fitted}{beezdemand_tmb}(
  object,
  scale = c("model", "natural"),
  level = c("subject", "population"),
  ...
)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} object.}

\item{scale}{One of \code{"model"} (default) or \code{"natural"}.}

\item{level}{One of \code{"subject"} (default; conditions on the subject
random effects) or \code{"population"} (random effects set to zero, giving
the population-mean values). See \code{\link[=predict.beezdemand_tmb]{predict.beezdemand_tmb()}}.}

\item{...}{Unused.}
}
\value{
Numeric vector of length \code{nobs(object)}.
}
\description{
Default returns fitted values on the model's native likelihood scale
(log scale for \code{"exponential"}, natural/LL4 scale for others), matching
\code{augment(fit)$.fitted}. Set \code{scale = "natural"} to back-transform.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
head(fitted(fit))
}
}
\seealso{
\code{\link[=predict.beezdemand_tmb]{predict.beezdemand_tmb()}}, \code{\link[=augment.beezdemand_tmb]{augment.beezdemand_tmb()}},
\code{\link[=residuals.beezdemand_tmb]{residuals.beezdemand_tmb()}}.
}
