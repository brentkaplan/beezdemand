% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{.tmb_build_emm_ref_grid}
\alias{.tmb_build_emm_ref_grid}
\title{Build the conditioned reference grid for TMB EMMs and comparisons}
\usage{
.tmb_build_emm_ref_grid(
  fit_obj,
  param = c("Q0", "alpha"),
  at = NULL,
  factors_in_emm = NULL
)
}
\arguments{
\item{fit_obj}{A \code{beezdemand_tmb} object.}

\item{param}{Character. \code{"Q0"} or \code{"alpha"}.}

\item{at}{Named list of factor-level filters or covariate-value overrides.}

\item{factors_in_emm}{Character subset of fitted factors to include.}
}
\value{
A list with components:
\describe{
\item{level_combos}{Filtered grid as a data.frame.}
\item{ref_X}{Filtered design matrix.}
\item{use_factors}{Character vector of factors driving the grid.}
\item{cov_names}{Character vector of continuous covariates.}
\item{is_intercept_only}{Logical; \code{TRUE} when the fit has neither
factors nor covariates.}
}
}
\description{
Shared helper that constructs \code{level_combos} (the factor-level grid,
optionally filtered by \code{at}) and \code{ref_X} (the corresponding design matrix)
for a TMB demand fit. Both \code{get_demand_param_emms.beezdemand_tmb()} and
\code{get_demand_comparisons.beezdemand_tmb()} consume this helper so they
cannot drift apart on which cells the user requested.
}
\details{
Continuous covariates are held at the training mean unless overridden via
\code{at}. Factor levels are filtered down to the requested values when \code{at}
names a factor.
}
\keyword{internal}
