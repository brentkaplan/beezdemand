% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-methods.R
\name{update.beezdemand_tmb}
\alias{update.beezdemand_tmb}
\title{Update a beezdemand_tmb fit}
\usage{
\method{update}{beezdemand_tmb}(object, ..., evaluate = TRUE)
}
\arguments{
\item{object}{A \code{beezdemand_tmb} object.}

\item{...}{Named arguments to substitute into the original
\code{fit_demand_tmb()} call.}

\item{evaluate}{If \code{TRUE} (default), re-evaluate the updated call and
return the new fit. If \code{FALSE}, return the unevaluated call.}
}
\value{
A new \code{beezdemand_tmb} object, or an unevaluated call.
}
\description{
Re-fits with named arguments substituted into the original call. Pass
any argument of \code{fit_demand_tmb()} (e.g., \code{factors = NULL},
\code{random_effects = ~ 1}, \code{equation = "simplified"}). Does NOT support
formula-update syntax (\code{. - term}) because \code{fit_demand_tmb()} is
argument-driven, not formula-driven.
}
\examples{
\donttest{
data(apt)
fit <- fit_demand_tmb(apt, equation = "exponential", verbose = 0)
update(fit, equation = "simplified", evaluate = FALSE)
}
}
\seealso{
\code{\link[=fit_demand_tmb]{fit_demand_tmb()}}, \code{\link[=formula.beezdemand_tmb]{formula.beezdemand_tmb()}}.
}
