% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross-price.R
\name{.cp_extract_convergence}
\alias{.cp_extract_convergence}
\title{Extract Backend Convergence Info from an NLS-family Fit}
\usage{
.cp_extract_convergence(model)
}
\arguments{
\item{model}{A fitted model object (\code{nls}, \code{nlsLM}, \code{nlsr}, or similar).}
}
\value{
A list with \code{isConv}, \code{finIter}, \code{stopCode}, \code{stopMessage}.
}
\description{
Internal helper (TICKET-065). Reads \code{model$convInfo} (present on \code{nls}/
\code{nlsLM}-class fits) into a plain list. \code{nlsr::wrapnlsr()} fits carry no
such diagnostic, so \code{isConv} is \code{NA} (unknown), not \code{FALSE} -- the two
must stay distinguishable so downstream gates only warn on an explicit,
known non-convergence.
}
\keyword{internal}
