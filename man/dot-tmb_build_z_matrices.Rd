% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tmb-demand.R
\name{.tmb_build_z_matrices}
\alias{.tmb_build_z_matrices}
\title{Build subject-level random-effects design matrices from a parsed RE spec}
\usage{
.tmb_build_z_matrices(re_parsed, data, id_var = "id")
}
\arguments{
\item{re_parsed}{Output of \code{.normalize_re_input()}.}

\item{data}{Long-format data frame the model is fit on.}

\item{id_var}{Subject id column (currently unused — reserved for
future per-subject reductions).}
}
\value{
A list with components:
\describe{
\item{Z_q0}{Numeric matrix, \code{n_obs} rows by \code{re_dim_q0} columns.}
\item{Z_alpha}{Numeric matrix, \code{n_obs} rows by \code{re_dim_alpha} columns.}
\item{re_dim_q0}{Integer; total Q0 RE columns.}
\item{re_dim_alpha}{Integer; total alpha RE columns.}
}
}
\description{
Consumes the canonical block representation produced by
\code{.normalize_re_input()} (Phase 1) and emits the per-observation design
matrices \code{Z_q0} and \code{Z_alpha} that the Phase-2 generalized
\code{src/MixedDemand.h} template will consume.
}
\details{
For each block, the helper extracts the \code{model.matrix()} columns
corresponding to that block's \code{terms_q0} / \code{terms_alpha} from \code{data}
and stacks them column-wise. Order: block 1 q0 cols, block 2 q0
cols, ..., then block 1 alpha cols, etc.

Intercept-only blocks (\verb{terms_*} is \code{"(Intercept)"}) produce a column
of 1s; factor-expanded blocks reproduce the model.matrix expansion.
}
\keyword{internal}
