% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/power-demand.R
\name{find_n_demand}
\alias{find_n_demand}
\title{Find the smallest sample size reaching a target power (demand)}
\usage{
find_n_demand(
  target_power = 0.8,
  effect = list(delta_q0 = NULL, delta_alpha = NULL),
  design = list(),
  n_range = c(6, 200),
  n_sim = 200,
  n_sim_max = 4 * n_sim,
  alpha = 0.05,
  df = NULL,
  seed = NULL,
  equation = "simplified",
  random_effects = nlme::pdDiag(Q0 + alpha ~ condition - 1),
  multi_start = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{target_power}{Target power in (0, 1).}

\item{effect}{Named list supplying exactly one of \code{delta_q0} or
\code{delta_alpha}: the true condition shift on natural-log Q0 (or natural-log
alpha) for condition 2 relative to condition 1. \code{0} is allowed (useful
for Type I error checks). E.g. \code{delta_q0 = log(1.5)} means condition 2's
Q0 is 1.5 times condition 1's.}

\item{design}{Named list of data-generating settings, merged over the
simulator defaults: \code{prices} (vector), \code{log_q0_pop}, \code{log_alpha_pop},
\code{sigma_b} (per-condition subject SD on log Q0), \code{sigma_d} (same for log
alpha), \code{rho_bd} (must be 0 in this version), and \code{sigma_e} (residual SD
on log consumption). All SDs must be strictly positive.}

\item{n_range}{Integer bracket \code{c(lower, upper)} to search
(\verb{2 <= lower < upper}). The search errors (rather than extrapolating) if
the target is not reached at \code{upper}.}

\item{n_sim}{Replicates per evaluation batch. Smaller than the
\code{\link[=power_demand]{power_demand()}} default because several N values are evaluated; the
adaptive rule adds batches where the verdict is close.}

\item{n_sim_max}{Maximum replicates per evaluated N (default \code{4 * n_sim}).}

\item{alpha}{Nominal two-sided test level.}

\item{df}{Degrees of freedom for the Wald test's t reference. \code{NULL}
(default) tracks the evaluated sample size as \code{n - 1}; a numeric value
(or \code{Inf} for the asymptotic z-test) is used at every evaluated N.}

\item{seed}{Optional integer seed; identical seeds give identical
results. The caller's RNG state is restored on exit.}

\item{equation}{Demand equation passed to \code{\link[=fit_demand_tmb]{fit_demand_tmb()}}. The default
\code{"simplified"} shares the simulator's mean function, so the simulated
\code{delta} and the fitted contrast share the same scale. Note the error
model is a \emph{working model}: the simulator draws multiplicative
lognormal errors while \code{"simplified"} fits additive Gaussian errors on
raw consumption; the approximation is closest at small \code{sigma_e}, and
Type I calibration is verified by the test suite at the default and a
3x-larger \code{sigma_e}. Other equations are sensitivity analyses with a
different estimand -- their contrasts are not on the scale of the
simulated delta.}

\item{random_effects}{Random-effects specification passed to
\code{\link[=fit_demand_tmb]{fit_demand_tmb()}}. The default \code{nlme::pdDiag(Q0 + alpha ~ condition - 1)}
(independent per-condition subject effects on both parameters) matches
the simulator's data-generating process when \code{rho_bd = 0}.}

\item{multi_start}{Passed to \code{\link[=fit_demand_tmb]{fit_demand_tmb()}}. Defaults to \code{FALSE} for
speed (roughly 3x fewer optimizations); non-convergent replicates are
excluded and surfaced rather than biasing the estimate.}

\item{verbose}{Logical; report each evaluation.}

\item{...}{Additional arguments passed to \code{\link[=fit_demand_tmb]{fit_demand_tmb()}}.}
}
\value{
An object of class \code{beezdemand_power_n}: a list with
\describe{
\item{n}{Estimated smallest \code{n_subjects} reaching \code{target_power};
\code{NA} when the confirmation pass contradicted the search
(\code{status = "unresolved"}).}
\item{target_power}{As supplied.}
\item{status}{\code{"confirmed"} (selected N re-confirmed above target and
N - 1 below), \code{"uncertain"} (a decision relied on a point estimate,
or N - 1 also cleared the target on reconfirmation so the returned
N may not be minimal), \code{"unresolved"} (the selected N failed its
own reconfirmation; \code{n} is \code{NA}), or \code{"at_lower_bound"} (the target
was already met at \code{n_range[1]}; smaller N was not explored). These
labels describe a heuristic Monte Carlo decision rule -- repeated
looks at ordinary Wilson intervals across several N -- not a formal
sequential error guarantee.}
\item{uncertain}{Logical; \code{TRUE} when any search decision was made on
a point estimate rather than a conclusive Wilson interval, or the
status is not \code{"confirmed"}/\code{"at_lower_bound"}.}
\item{evaluations}{Tibble of every evaluation: \code{n_subjects},
\code{n_sim_total}, \code{n_used}, \code{usable_fraction}, \code{power}, \code{ci_lower},
\code{ci_upper}, \code{decision}. A warning fires when any evaluation had
fewer than 95\% usable fits.}
\item{alpha, df, effect, design, n_range, n_sim, n_sim_max, seed,
settings, call}{Echoed inputs and effective settings.}
}
}
\description{
Bisection search over \code{n_subjects} for the smallest N whose Monte Carlo
power estimate from \code{\link[=power_demand]{power_demand()}} reaches \code{target_power}. The search
accounts for Monte Carlo noise: at each evaluated N, replicates are added
in batches (up to \code{n_sim_max}) until the Wilson interval for power lies
wholly above or below the target; if it still straddles the target at the
cap, the decision falls back to the point estimate and the result is
flagged \code{uncertain}. The selected N and its lower neighbor are then
re-evaluated with fresh replicates before minimality is claimed.

The returned \code{n} is an \emph{estimated minimum under Monte Carlo uncertainty},
not an exact bound. For grant-quality reporting, rerun \code{\link[=power_demand]{power_demand()}} at
the returned \code{n} with a large \code{n_sim} (2000+) and report that estimate
with its Monte Carlo confidence interval.
}
\examples{
\donttest{
# Small search for demonstration (use larger n_sim for real planning)
res <- find_n_demand(
  target_power = 0.8,
  effect = list(delta_q0 = log(2.5)),
  n_range = c(4, 30), n_sim = 30, seed = 1, verbose = FALSE
)
print(res)
}

}
\seealso{
\code{\link[=power_demand]{power_demand()}} for the Monte Carlo engine.

Other power-analysis: 
\code{\link{power_demand}()}
}
\concept{power-analysis}
