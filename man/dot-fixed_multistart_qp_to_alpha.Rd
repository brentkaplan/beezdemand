% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fixed-multistart.R
\name{.fixed_multistart_qp_to_alpha}
\alias{.fixed_multistart_qp_to_alpha}
\title{Map sampled (Q0, Pmax) to alpha using the package's own closed forms}
\usage{
.fixed_multistart_qp_to_alpha(equation, k_nat, q0, pmax)
}
\arguments{
\item{equation}{\code{"hs"}, \code{"koff"}, or \code{"simplified"}.}

\item{k_nat}{Natural-scale \code{k} (ignored for \code{"simplified"}).}

\item{q0}{Numeric vector (or scalar, recycled) of sampled/fixed Q0.}

\item{pmax}{Numeric vector of sampled Pmax.}
}
\value{
Numeric vector of alpha starting values (same length as \code{pmax}).
}
\description{
Mirrors \code{beezdemand_calc_pmax_omax()} (\code{R/pmax-omax-engine.R}), verified
by round-trip: fixed-effect \code{"hs"} and \code{"koff"} fit the identical mean
function (\code{Q(p) = Q0 * 10^(k * (exp(-alpha * Q0 * p) - 1))}, just on
different scales) and share the Lambert-W relation used by
\code{.pmax_analytic_hs()}:
\code{Pmax = -W_0(-1 / (k * ln(10))) / (alpha * Q0)}
\verb{=> alpha = -W_0(-1 / (k * ln(10))) / (Pmax * Q0)}.
\code{"simplified"}/SND uses \code{.pmax_analytic_snd()}'s closed form:
\verb{Pmax = 1 / (alpha * Q0) => alpha = 1 / (Pmax * Q0)}.
}
\details{
Low-k fallback (Codex 2F review fold, item 5): if \code{k} is too small for a
real principal-branch Lambert-W solution to exist at this scale (\code{k <= exp(1) / log(10)}, mirroring \code{.pmax_analytic_hs()}'s own existence check,
or if \code{lambertW()} itself fails), hs/koff have no interior Pmax at all --
the sampled Pmax is not achievable by ANY alpha via the closed form. The
function does NOT silently fall back to the (unrelated) SND point
formula \code{alpha = 1 / (Pmax * Q0)} in this case. Instead it draws alpha
directly and independently, log-uniform over a range implied by the same
sampled (Q0, Pmax) bounds -- an explicit, documented, genuinely
stochastic low-k/degenerate-k sampler (two calls with the same inputs
but different RNG state give different alpha).
}
\keyword{internal}
