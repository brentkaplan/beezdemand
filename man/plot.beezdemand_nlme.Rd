% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mixed-methods.R
\name{plot.beezdemand_nlme}
\alias{plot.beezdemand_nlme}
\title{Plot Method for beezdemand_nlme Objects}
\usage{
\method{plot}{beezdemand_nlme}(
  x,
  y = NULL,
  show_observed_data = TRUE,
  observed_point_alpha = 0.6,
  show_pred_lines = TRUE,
  n_points_pred = 100,
  inv_fun = identity,
  facet_formula = NULL,
  color_by = NULL,
  linetype_by = NULL,
  shape_by = NULL,
  x_trans = "identity",
  y_trans = "identity",
  title = NULL,
  xlab = NULL,
  ylab = NULL,
  observed_point_size = 2,
  pop_line_size = 1,
  ind_line_size = 0.6,
  pop_line_alpha = 0.9,
  ind_line_alpha = 0.3,
  ...
)
}
\arguments{
\item{x}{A `beezdemand_nlme` object.}

\item{y}{Ignored. Placeholder for S3 consistency.}

\item{show_observed_data}{Logical. If TRUE, plots the original data points. Default `TRUE`.}

\item{observed_point_alpha}{Alpha for observed points. Default `0.6`.}

\item{show_pred_lines}{Logical or Character Vector.
If `TRUE` (or `"population"`), plots population-level prediction lines.
If `"individual"`, plots individual/group-level prediction lines.
If `c("population", "individual")`, plots both. Default `TRUE` (which means "population").}

\item{n_points_pred}{Integer. Number of points for prediction lines. Default `100`.}

\item{inv_fun}{Optional function to inverse-transform y-axis and predictions. Default `identity`.}

\item{facet_formula}{Optional faceting formula (e.g., `~ dose`).}

\item{color_by}{Optional character string: name of a factor to color lines and/or points by.
Must be a column in `x$data`.}

\item{linetype_by}{Optional character string: name of a factor for linetypes of population prediction lines
if individual lines are also shown (otherwise applies to the shown lines).
Must be a model factor in `x$param_info$factors`.}

\item{shape_by}{Optional character string: name of a factor for shapes of observed points.
Must be a column in `x$data`.}

\item{x_trans}{Character. Transformation for x-axis. Default "identity".}

\item{y_trans}{Character. Transformation for y-axis. Default "identity".}

\item{title}{Optional plot title.}

\item{xlab}{Optional x-axis label.}

\item{ylab}{Optional y-axis label.}

\item{observed_point_size}{Size for observed points. Default `2`.}

\item{pop_line_size}{Size for population prediction lines. Default `1`.}

\item{ind_line_size}{Size for individual prediction lines. Default `0.6`.}

\item{pop_line_alpha}{Alpha for population prediction lines. Default `0.9`.}

\item{ind_line_alpha}{Alpha for individual prediction lines. Default `0.3`.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object.
}
\description{
Creates a ggplot2 visualization of a fitted `beezdemand_nlme` model,
showing observed data points and/or model prediction lines.
}
